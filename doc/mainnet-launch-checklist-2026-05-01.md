# BlackRaven Mainnet Launch Checklist (2026-05-01)

Owner: launch team  
Branch: `launch-kawpow-genesis`  
Code freeze target: immediate (consensus-critical)

## 1) Consensus and Chain Identity

- [x] Mainnet is default when no chain flag is provided.
- [x] `-testnet` and `-regtest` are opt-in only.
- [x] Confirm mainnet message start bytes in `src/chainparams.cpp` (`42 4c 4b 52`).
- [x] Confirm mainnet default P2P and RPC ports in `src/chainparams*.cpp` (`18768` / `18769`).
- [x] Confirm genesis hash value is finalized in `src/chainparams.cpp` assertions.
- [ ] Confirm subsidy schedule and halving interval values are final.
- [ ] Confirm seed nodes are reachable and stable.

## 2) Build and Artifact Integrity

- [ ] Clean Linux build (daemon/cli/qt) succeeds.
- [x] Clean Windows cross-build (daemon/cli/qt) succeeds.
- [x] `blackravend`, `blackraven-cli`, and `blackraven-qt`/`.exe` present in release dirs.
- [x] SHA256 manifest generated for selected mainnet prelaunch artifacts.
- [ ] Optional: signed checksum manifest (`SHA256SUMS.asc`) generated by release key.

## 3) Runtime Smoke Validation

- [ ] Linux: `blackravend --version` works.
- [ ] Linux: `blackraven-cli --version` works.
- [ ] Linux: `blackraven-qt --version` works.
- [x] Windows Qt launch workaround validated (`-splash=0 -resetguisettings`); safe launcher added in release zip.
- [ ] Wallet create/load/send/receive smoke tested.

## 4) Network Readiness

- [ ] At least 3 bootstrap/full nodes prepared for launch window.
- [ ] Explorer/indexer synced on final branch and validated.
- [ ] Mining path validated on final params (block template, submit, reward).
- [ ] Monitoring in place (height, peers, mempool, orphan/fork alerts).

## 5) Release Ops

- [ ] Launch artifacts uploaded to GitHub release draft.
- [ ] Website download links staged and verified.
- [ ] Public checksum/signature verification instructions published.
- [ ] Launch announcement draft finalized.
- [ ] Incident response contacts and escalation path confirmed.

## 6) Go / No-Go

- [ ] Final review completed (T-24h).
- [ ] Release tag approved and created.
- [ ] "Go" call at T-60m before launch.
